spring:
  application:
    name: spring-jpa-audit

  datasource:
    url: jdbc:mysql://localhost:3306/poc_audit_test
    username: root
    password: rootadmin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true